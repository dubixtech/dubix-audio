
{
  "name": "esp32-audio-upload-server",
  "version": "1.0.0",
  "description": "ESP32 audio upload server using Fastify",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "fastify": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
